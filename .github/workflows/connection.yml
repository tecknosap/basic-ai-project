# name: Azure OIDC Terraform Deploy

# on:
#   push:
#     branches:
#       - main

# permissions:
#   id-token: write
#   contents: read

# jobs:
#   deploy:
#     runs-on: ubuntu-latest      

#     env:
#       ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
#       ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
#       ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#       ARM_USE_OIDC: true

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Azure Login via OIDC
#         uses: azure/login@v1
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Set up Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: 1.6.6

#       # 🚀 Terraform Execution
#       - name: Terraform Init
#         run: terraform init -input=false -reconfigure
#         working-directory: ./terraform

#       - name: Terraform Validate
#         run: terraform validate
#         working-directory: ./terraform

#       - name: Terraform Plan
#         run: |
#           terraform plan -input=false -out=tfplan -var="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}"
#         working-directory: ./terraform

#       - name: Terraform Apply
#         run: terraform apply -input=false -auto-approve tfplan
#         working-directory: ./terraform



name: Azure OIDC Terraform + Flask Deploy

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Azure Login via OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.6.6

      - name: Terraform Init
        run: terraform init -input=false -reconfigure
        working-directory: ./terraform

      # - name: Terraform Validate
      #   run: terraform validate
      #   working-directory: ./terraform

      - name: Terraform Plan
        run: |
          terraform plan -input=false -out=tfplan -var="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}"
        working-directory: ./terraform

      - name: Terraform Apply
        run: terraform apply -input=false -auto-approve tfplan
        working-directory: ./terraform

      # # Python Setup
      # - name: Setup Python
      #   uses: actions/setup-python@v4
      #   with:
      #     python-version: 3.11

      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -r app/requirements.txt

      # - name: Deploy Flask app to App Service
      #   uses: azure/webapps-deploy@v2
      #   with:
      #     app-name: gocloudops-app-service
      #     package: ./app
